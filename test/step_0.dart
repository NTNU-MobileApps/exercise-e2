import 'package:test/test.dart';

/// This is a dummy test for the GitHub workflow on each commit to the master branch
void main() {
  test('Step 1 - Animal class', () {
    expect(6 * 7, 42);
  });
}
